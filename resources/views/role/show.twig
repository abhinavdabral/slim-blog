{% extends 'templates/app.twig' %}

{% block content %}
<div class="row">
    
    <div class="page-header">
        <h1>{{role.name}}</h1>
    </div>
    <br>
    <br>
    <a href="{{path_for('role')}}" class="btn btn-primary">Back</a>
</div>
{% endblock %}

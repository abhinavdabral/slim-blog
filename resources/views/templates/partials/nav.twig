<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="{{ path_for('home') }}">Slim Blog</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
      {% if auth.check %}
        <li><a href="{{ path_for('home') }}">Dashboard</a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> Posts <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="{{path_for('post')}}">All Posts</a></li>
            <li role="separator" class="divider"></li>                
            <li><a href="{{path_for('post.new')}}">New Post</a></li>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> Users <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="{{path_for('user')}}">All Users</a></li>
            <li role="separator" class="divider"></li>                
            <li><a href="{{path_for('user.new')}}">New User</a></li>
          </ul>
        </li>
      {% endif %}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {% if auth.check %}
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> {{ auth.user.name }} <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="{{ path_for('auth.password.change') }}">Change Password</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="{{ path_for('auth.signout') }}">Sign Out</a></li>
          </ul>
        </li>
        {% else %}
        <li><a href="{{ path_for('auth.signup') }}">Sign Up</a></li>
        <li><a href="{{ path_for('auth.signin') }}">Sign In</a></li>
        {% endif %}
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>
